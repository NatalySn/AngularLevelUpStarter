import { AttributeDirective } from './attribute.directive';

describe('AttiburteDirective', () => {
  it('should create an instance', () => {
    const directive = new AttributeDirective();
    expect(directive).toBeTruthy();
  });
});
